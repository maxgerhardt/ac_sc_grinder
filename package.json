{
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "build": "npm run lint && ./scripts/build_config.js"
  },
  "devDependencies": {
    "eslint": "^5.1.0",
    "js-yaml": "^3.12.0"
  }
}
